const initialState = {
    list: [
        {title: 'Primeira nota', body: 'testando 123'}
    ]
};

export default (state = initialState, action) => {

    switch(action.type) {
        case 'ADD_NOTE':
        
        break;
    }

    return state;
}